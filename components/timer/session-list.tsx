// 'use client'
// import {SessionItem} from './session-item'

// import type {TimerSessionListProps} from '@/lib/types'

// export function SessionList({timerSessions, onDeleteSession}: TimerSessionListProps) {
//   return timerSessions.length > 0 ? (
//     <ul className='space-y-2'>
//       {timerSessions.map((session) => (
//         <SessionItem key={session.id} timerSession={session} onDelete={onDeleteSession} />
//       ))}
//     </ul>
//   ) : (
//     <p className='text-muted-foreground'>No Pomodoro sessions yet.</p>
//   )
// }
